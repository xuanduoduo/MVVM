package com.chenxuan.common.utils.router

/**
 * @author cx
 */
interface RouterPath {
    companion object {
        const val APP_LAUNCHER = "/app/launcher"

        const val LOGIN_MAIN = "/login/main"
        const val LOGIN_SCAN = "/login/scan"
        const val LOGIN_WIDGET = "/login/widget"
    }
}